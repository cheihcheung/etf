# 项目概述

> 更新时间: 2026 年 5 月 17 日

## 相关文档

- [前端开发规范](02-前端开发规范.md) - 前端技术栈和开发规范
- [后端开发规范](03-后端开发规范.md) - 后端技术栈和开发规范
- [数据库设计](04-数据库设计文档.md) - 数据库表结构设计
- [API 接口文档](05-API接口文档.md) - 接口定义和调用
- [策略算法文档](06-策略算法文档.md) - 核心策略算法原理
- [部署文档](07-部署文档.md) - 项目部署指南

---

## 1. 项目简介

**项目名称**: ETF 多资产动态配置策略系统

**项目定位**: 基于前后端分离架构的 ETF 量化回测与参数寻优平台，支持多资产动态配置、双策略并行执行、历史区间回测与可视化分析。

**当前状态**: 核心功能开发完成，支持完整回测流程与参数寻优

**技术架构**:

- 前端: Vue 3 + TypeScript + Vite + Element Plus + ECharts
- 后端: Node.js + Express + 极简 ORM 模型层
- 数据库: MySQL 8.0

---

## 2. 技术栈

### 2.1 前端技术栈

| 技术 | 版本 | 说明 |
| --- | --- | --- |
| Node.js | 20.x | 运行环境 |
| TypeScript | 5.x | 类型安全 |
| Vue | 3.5.x | 核心框架 |
| Vite | 5.x | 构建工具 |
| Vue Router | 4.x | 路由管理 |
| Element Plus | 2.x | UI 组件库 |
| ECharts | 5.x | 图表可视化 |
| Axios | 1.x | HTTP 客户端 |

### 2.2 后端技术栈

| 技术 | 版本 | 说明 |
| --- | --- | --- |
| Node.js | 20.x | 运行环境 |
| Express | 4.x | Web 框架 |
| MySQL2 | 3.x | 数据库驱动 |
| Axios | 1.x | HTTP 客户端(爬虫) |

### 2.3 数据库

| 技术 | 版本 | 说明 |
| --- | --- | --- |
| MySQL | 8.0+ | 主数据库 |

---

## 3. 项目结构

```
etf/
├── client/                          # 前端工程
│   └── src/
│       ├── api/                    # API 接口层
│       ├── components/             # 公共组件
│       ├── router/                 # 路由配置
│       ├── types/                  # TypeScript 类型定义
│       ├── utils/                  # 工具函数
│       └── views/                  # 页面组件
│           ├── Backtest.vue        # 回测页面
│           ├── EtfConfig.vue       # ETF 配置页
│           ├── EtfHistory.vue      # ETF 历史 K 线页
│           └── StrategyConfig.vue  # 策略配置页
├── server/                          # 后端工程
│   └── src/
│       ├── config/                 # 配置文件
│       ├── models/                 # 数据模型层(ORM)
│       ├── routes/                 # 路由控制器层
│       ├── services/               # 服务层(核心逻辑)
│       ├── utils/                  # 工具函数
│       └── index.js                # 应用入口
├── database/                        # 数据库脚本
│   └── backtest_results.sql
└── docs/                            # 项目文档
```

---

## 4. 核心功能模块

### 4.1 已实现模块

#### ETF 资产管理

- ETF 标的添加、编辑、删除
- 实时行情同步
- 历史日 K 线数据同步
- 初始配比配置(强制校验总占比 100%)

#### 策略配置

- 全局基础参数(回测区间、初始资金、交易费率等)
- 策略 A: 净值高低点回撤/反弹档位策略(全参数可自定义)
- 策略 B: 长期年化中枢偏离估值策略(全参数可自定义)
- 日常再平衡机制(偏离阈值可配置)
- 双策略独立开关与优先级设置

#### 历史回测

- 自定义回测区间与初始资金
- 逐日时间轴推进引擎
- 延迟补仓机制(支持上市首日建仓)
- 全套量化指标(总收益率、年化收益、最大回撤、夏普比率等)
- 基准(沪深300)对比分析

#### 参数寻优

- 支持再平衡阈值自动遍历
- 寻优结果排序与展示
- 防爆盘保护(isOptimization 标记)

#### 数据可视化

- 组合净值曲线
- 沪深300 基准对比曲线
- 交易流水明细
- 每日调仓日志

### 4.2 规划中模块

- 移动端 H5 适配
- 实盘模拟交易
- 多策略组合回测对比
- 自定义策略脚本

---

## 5. 系统架构

### 5.1 MVC 三层架构

系统采用严谨的 MVC 分层设计:

| 层级 | 目录 | 职责 |
| --- | --- | --- |
| 路由控制层 | `routes/` | HTTP 请求分发、参数校验、响应输出 |
| 模型层 | `models/` | 数据库读写、ORM 封装 |
| 服务层 | `services/` | 核心算法、策略引擎、爬虫 |

### 5.2 核心业务流程

```
启动回测参数 -> 锁定基准(510300)
  -> 读取历史行情 -> 首日建仓
  -> 逐日循环: 更新价格 -> 延迟补仓
  -> 策略A/B信号研判 -> 决策融合
  -> 日常再平衡 -> 推进下一日
  -> 循环结束 -> 计算量化指标 -> 存盘输出
```

### 5.3 数据库关系

系统共 7 张物理表，以 `etf_basic` 为核心关联 `etf_history`(历史行情)、`trade_records`(交易流水)、`backtest_results`(回测结果)，辅以 `global_config`(全局配置)、`strategy_a_config`(策略A配置)、`strategy_b_config`(策略B配置)。

---

## 6. 开发环境

### 6.1 必需工具

- Node.js 20+
- MySQL 8.0+
- Git
- VS Code (推荐)

### 6.2 快速启动

```bash
# 数据库初始化
mysql -u root -p < database/backtest_results.sql

# 后端启动
cd server
npm install
npm run dev    # 默认端口 3001

# 前端启动
cd client
npm install
npm run dev    # 默认端口 8088
```

---

## 7. 技术特点

### 7.1 极简 ORM

参考 ThinkPHP 风格的万能 ORM 基类 `BaseModel.js`，所有数据库读写通过统一模型基类完成，杜绝裸写 SQL 注入风险。

### 7.2 内存沙盒回测

回测引擎在内存沙盒中闭环运行，逐日推进，所有计算不依赖数据库事务，确保无"未来数据穿越"的学术漏洞。

### 7.3 防爆盘保护

海量参数寻优时通过 `isOptimization` 标记禁止写入大 JSON 字段，防止数据库爆盘。

### 7.4 决策融合引擎

多策略冲突时通过优先级机制合并决策，避免同一交易日多次调仓造成资产擦除与双重手续费。

### 7.5 统一规范

| 规范项 | 约定 |
| --- | --- |
| HTTP 方法 | GET 用于查询，POST 用于创建/执行，PUT 用于更新，DELETE 用于删除 |
| 参数命名 | API 请求统一使用 camelCase，数据库字段统一使用 snake_case |
| 响应格式 | 统一 JSON 格式：`{ success: boolean, data: any, message: string }` |
| 错误处理 | 路由层统一 try-catch 包裹，错误通过 `res.status(500).json({ success: false, message })` 返回 |
| 数据模型 | 所有数据库操作通过 BaseModel 完成，禁止裸写 SQL |

### 7.6 安全机制

- **SQL 注入防护**: 通过 BaseModel 参数绑定机制防注入
- **参数校验**: 前端使用 TypeScript 类型校验，后端在路由层进行必要参数校验
- **事务保护**: 配置批量更新使用数据库事务保证一致性
- **日志记录**: 日志记录: 关键操作(添加/删除 ETF、配置变更、回测执行)记录操作日志

---

## 8. 团队协作

### 8.1 开发规范

- 遵循项目文档规范，保持代码风格一致
- 前后端 API 遵循统一接口规范
- 代码提交前运行 lint 检查
- 重要功能开发完成后需验证回测流程正常

### 8.2 分支管理

- `main` — 生产环境分支
- `develop` — 开发环境分支
- `feature/*` — 功能分支，如 `feature/backtest-optimization`
- `fix/*` — 修复分支，如 `fix/ratio-precision`

### 8.3 提交规范

```
<type>(<scope>): <subject>

feat(backtest): 添加延迟补仓机制
fix(config): 修复再平衡阈值不生效
docs(api): 更新API文档
refactor(model): 重构 BaseModel 查询方法
style(ui): 调整回测结果表格样式
chore(deps): 更新依赖版本
```

**type 类型说明**:

| 类型 | 说明 |
| --- | --- |
| feat | 新功能 |
| fix | 修复 Bug |
| docs | 文档更新 |
| refactor | 代码重构 |
| style | 样式调整 |
| chore | 构建/依赖变更 |

### 8.4 项目进度

#### 当前阶段

- 基础架构搭建完成
- 核心回测引擎开发完成
- 双策略算法验证通过
- 参数寻优功能完成

#### 后续计划

- ETF 历史数据自动补全优化
- 回测结果的图表交互增强
- 多组合并行回测
- 回测性能优化(大数据量加速)